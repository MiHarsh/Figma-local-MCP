{
  "name": "framelink-figma-plugin",
  "version": "1.3.0",
  "private": true,
  "description": "Figma plugin to export design data + assets locally for the figma-local-mcp server",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.24.0"
  }
}
